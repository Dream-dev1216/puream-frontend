import theme from './theme'
import navigation from './navigation'

export default {
  // product display information
  product: {
    name: 'DD',
    version: '1.0.0'
  },

  // theme configs
  theme,

  // navigation configs
  navigation
}
